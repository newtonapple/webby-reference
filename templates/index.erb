---
browser_title:  <%= title %>
title:          <%= title %>
name:           <%= title %>
created_at:     <%= Time.now.to_y %>
filter:
  - wikiwords
  - erb
  - textile
---

<%%= render :partial => "<%= directory %>/index" %>
